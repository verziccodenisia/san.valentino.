<!DOCTYPE html>
<html lang="it">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Andrea, vuoi essere il mio Valentino? 💖</title>

<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Poppins", system-ui, sans-serif;
}

body {
  height: 100vh;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  background: linear-gradient(135deg,#ffd6e0,#ffeaf0,#ffc2d1);
  position: relative;
}

/* ===== CONTENITORE ===== */
.container {
  text-align: center;
  z-index: 2;
}

h1 {
  font-size: 3rem;
  color: #b30059;
  margin-bottom: 50px;
}

/* ===== AREA BOTTONI ===== */
.buttons {
  position: relative;
  width: 360px;
  height: 180px;
  margin: auto;
}

/* ===== SI ===== */
#yesBtn {
  background: linear-gradient(45deg,#ff4d6d,#ff758f);
  color: white;
  border: none;
  padding: 20px 52px;
  font-size: 1.4rem;
  border-radius: 50px;
  cursor: pointer;
  transition: .25s;
  box-shadow: 0 8px 24px rgba(255,0,90,.4);
}

#yesBtn:hover {
  transform: scale(1.18);
}

/* ===== NO PAZZO ===== */
#noBtn {
  position: absolute;
  left: 200px;
  top: 0;
  background: white;
  border: 2px solid #ff4d6d;
  color: #ff4d6d;
  padding: 14px 36px;
  border-radius: 40px;
  cursor: pointer;
  transition: transform .15s;
}

/* ===== MESSAGGIO ===== */
#message {
  display: none;
  margin-top: 40px;
  font-size: 2rem;
  color: #b30059;
  animation: pop .4s ease;
}

@keyframes pop {
  from { transform: scale(0); }
  to { transform: scale(1); }
}

/* ===== CUORI BACKGROUND ===== */
.heart {
  position: absolute;
  color: #ff6b81;
  animation: floatUp linear infinite;
}

@keyframes floatUp {
  from { transform: translateY(100vh); opacity: 0; }
  20% { opacity: 1; }
  to { transform: translateY(-10vh); opacity: 0; }
}

/* ===== CORIANDOLI ===== */
.confetti {
  position: absolute;
  width: 8px;
  height: 14px;
  background: red;
  opacity: .9;
  animation: fall 2.5s linear forwards;
}

@keyframes fall {
  to {
    transform: translateY(100vh) rotate(720deg);
    opacity: 0;
  }
}
</style>
</head>

<body>

<div class="container">
  <h1>Andrea… vuoi essere il mio San Valentino? 💘</h1>

  <div class="buttons">
    <button id="yesBtn">SÌ 💖</button>
    <button id="noBtn">NO 😈</button>
  </div>

  <div id="message">Lo sapevo Andrea!! 🥰✨</div>
</div>

<script>
const noBtn = document.getElementById("noBtn");
const yesBtn = document.getElementById("yesBtn");
const message = document.getElementById("message");
const container = document.querySelector(".buttons");

let scale = 1;

/* ===== NO MINI-GIOCO ===== */
function moveCrazy() {
  const maxX = container.clientWidth - noBtn.offsetWidth;
  const maxY = container.clientHeight - noBtn.offsetHeight;

  const x = Math.random() * maxX;
  const y = Math.random() * maxY;

  scale *= 0.9; // si rimpicciolisce
  const rotate = Math.random() * 360;

  noBtn.style.left = x + "px";
  noBtn.style.top = y + "px";
  noBtn.style.transform = `scale(${scale}) rotate(${rotate}deg)`;
}

noBtn.addEventListener("mouseenter", moveCrazy);
noBtn.addEventListener("click", moveCrazy);

/* ===== CLICK SI ===== */
yesBtn.addEventListener("click", () => {
  message.style.display = "block";
  yesBtn.style.display = "none";
  noBtn.style.display = "none";

  launchConfetti();
});

/* ===== CUORI CONTINUI ===== */
function createHeart() {
  const heart = document.createElement("div");
  heart.className = "heart";
  heart.innerText = "❤";

  heart.style.left = Math.random()*100 + "vw";
  heart.style.fontSize = (12 + Math.random()*22) + "px";
  heart.style.animationDuration = (4 + Math.random()*4) + "s";

  document.body.appendChild(heart);
  setTimeout(()=>heart.remove(), 8000);
}

setInterval(createHeart, 300);

/* ===== CORIANDOLI ===== */
function launchConfetti() {
  for(let i=0;i<180;i++){
    const c = document.createElement("div");
    c.className = "confetti";

    const colors = ["#ff4d6d","#ff758f","#ffb3c6","#fff","#ff8fab"];
    c.style.background = colors[Math.floor(Math.random()*colors.length)];

    c.style.left = Math.random()*100 + "vw";
    c.style.top = "-20px";

    document.body.appendChild(c);

    setTimeout(()=>c.remove(), 2500);
  }
}
</script>

</body>
</html>
